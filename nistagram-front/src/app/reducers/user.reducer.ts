/*
import * as UserActions from '../actions/user.actions';
import {UserModel} from '../models/user.model';

export type Action = UserActions.All;

export function actionReducer(state: UserModel, action: Action): UserModel{
  switch (action.type){
    case UserActions.LOAD_MY_PROFILE_FAIL:
      return{...state};
    case UserActions.LOAD_MY_PROFILE_SUCCESS:
      return {...state};
    case UserActions.LOAD_MY_PROFILE:
      return {...state};
    default:
      return state;
  }
}

*/
